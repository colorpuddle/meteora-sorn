# Production Environment Variables Template
# DO NOT commit actual secrets to git!
# Copy this file and fill in your production values

# ============================================
# PUBLIC VARIABLES (Exposed in Client Bundle)
# ============================================
# These will be embedded in the frontend JavaScript bundle
# Only include non-sensitive configuration here

NEXT_PUBLIC_NETWORK=mainnet-beta
NEXT_PUBLIC_EXPLORER_URL=https://explorer.solana.com
NEXT_PUBLIC_POOL_CONFIG_KEY=YOUR_MAINNET_POOL_CONFIG_KEY

# Public RPC URL (frontend transactions, can be rate-limited)
# Use a free public endpoint here since it's exposed to clients
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Cloudflare Worker URL (if using worker-based uploads)
NEXT_PUBLIC_UPLOAD_WORKER_URL=https://your-upload-worker.your-subdomain.workers.dev

# ============================================
# PRIVATE VARIABLES (Server-Side Only)
# ============================================
# These are ONLY available in API routes and server-side code
# NEVER exposed to the client

# Private RPC URL (API routes, paid/premium endpoint)
# Use your paid RPC provider here (Helius, QuickNode, etc.)
RPC_URL=https://your-premium-rpc-endpoint.com/YOUR_API_KEY

# Pool Configuration (fallback for API routes)
POOL_CONFIG_KEY=YOUR_MAINNET_POOL_CONFIG_KEY

# Marketplace Configuration
MARKETPLACE_WALLET=YOUR_MARKETPLACE_WALLET_ADDRESS

# ============================================
# ARWEAVE CONFIGURATION
# ============================================
# Arweave wallet private key (JWK format)
# CRITICAL: Keep this secret! Used to sign Arweave transactions
ARWEAVE_WALLET_KEY={"d":"...","dp":"...","dq":"...","e":"AQAB","kty":"RSA","n":"...","p":"...","q":"...","qi":"..."}

# ============================================
# CLOUDFLARE R2 CONFIGURATION
# ============================================
# R2 credentials for encrypted keypair storage
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens

R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_BUCKET=meteora-srn-keypairs-v2

# ============================================
# ENCRYPTION MASTER KEY
# ============================================
# Master key for decrypting R2 keypairs (64 hex characters = 32 bytes)
# CRITICAL: This must match the key used to encrypt your R2 keypairs!
MASTER_KEY=your_64_character_hex_master_key_here

# ============================================
# SECURITY CHECKLIST
# ============================================
# ✅ All .env files are in .gitignore
# ✅ No NEXT_PUBLIC_ prefix on sensitive variables
# ✅ RPC_URL uses premium endpoint (server-side)
# ✅ NEXT_PUBLIC_RPC_URL uses public endpoint (client-side)
# ✅ ARWEAVE_WALLET_KEY is kept secret
# ✅ R2 credentials are kept secret
# ✅ MASTER_KEY is kept secret
# ✅ Production values configured in Cloudflare Pages dashboard
